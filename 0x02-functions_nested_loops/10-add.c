#include "main.h"
#include <stdio.h>

/**
 * add - is a function that add the first number to the secone.
 * @x: the first parameter passed to the function.
 * @y: the second parameter passed to the function.
 * Return: the sum of the 2 parameters.
 */

int add(int x, int y)
{
	return (x + y);
}

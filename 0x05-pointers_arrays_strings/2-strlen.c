#include "main.h"

/**
 * _strlen - a function return length of a string.
 * @s: string passed as a parameter.
 * Return: int count of chars.
 */

int _strlen(char *s)
{
	int len;

	while (s[len] != '\0')
	{
		len++
	}
	return (len);
}
